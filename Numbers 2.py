import math
# to round a number we use function:
print(round(2.5))
# to conv. the neg. num to pos. we use:
print(abs(-2.9))

# in python we can deal with complex number by importing
# a module (import math)
print(math.ceil(2.2))
print(math.floor(2.5))
print(math.log(56))
print(math.cos(30)*2)
